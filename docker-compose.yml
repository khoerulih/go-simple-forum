services:
  db:
    image: mysql:latest
    container_name: simple-forum-sql
    ports:
      - "3307:3306" # access port 3307 host but in container, it still use 3306
    environment:
      - MYSQL_ROOT_PASSWORD=2secret
      - MYSQL_DATABASE=db-simple-forum
    volumes:
      - /home/khoerulih/belajar-golang/go-project/volume-backup/go-simple-forum-sql/db:/var/lib/mysql